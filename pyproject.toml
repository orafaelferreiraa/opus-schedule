[tool.pytest.ini_options]
# Permite rodar `pytest` da raiz do repositório: coloca `src/` no sys.path
# (os testes fazem `from shared... import`).
pythonpath = ["src"]
testpaths = ["src/tests"]
addopts = "-q"
